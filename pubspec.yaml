name: data_layer
description: A starting point for Dart libraries or applications.
version: 1.0.1
publish_to : none
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  http: ^0.13.5
  html: ^0.15.1
  jwt_decoder: ^2.0.1
  
dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
